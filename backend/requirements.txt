# ── Web Framework ──
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# ── Config ──
python-dotenv>=1.0.0
pydantic>=2.5.3
pydantic-settings>=2.0.0

# ── AI / Agent ──
# IMPORTANT: These must be from the same "generation" of LangChain.
# langchain-google-genai 2.x needs langchain-core 0.3.x.
# google-genai powers the direct video-analysis path in gemini_analyzer.py.
# Do NOT pin langchain-core separately — let pip resolve it from these two.
langchain-google-genai>=2.0.0,<3.0.0
langchain-openai>=0.2.0,<1.0.0
langgraph>=0.2.0,<1.0.0
langgraph-checkpoint-postgres>=3.0.0,<4.0.0
google-generativeai>=0.8.0,<0.9.0
google-genai>=1.65.0,<2.0.0

# ── Video / Services ──
yt-dlp>=2024.1.1
httpx>=0.26.0
Pillow>=10.0.0

# ── Search ──
duckduckgo-search>=4.0.0
tavily-python>=0.3.0

# ── Database ──
supabase>=2.0.0

# ── Browser Automation ──
playwright>=1.52.0
browser-use>=0.1.0
# playwirght install / 

# ── Testing ──
pytest>=7.4.4
pytest-asyncio>=0.23.3
